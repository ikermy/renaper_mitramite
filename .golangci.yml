run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - gofmt
    - goimports
    - misspell
    - gocritic
    - revive

issues:
  exclude-use-default: false

